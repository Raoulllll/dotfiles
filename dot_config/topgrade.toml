[commands]
"System Configuration Backup" = "sh /home/roehl/.local/bin/cz-sync.sh"
"NixOS Flake Rebuild" = "rebuild"

[post_commands]
"NixOS Storage Cleanup" = "sudo nix-collect-garbage -d"

[misc]
disable = ["firmware", "nix", "system", "containers", "spicetify", "chezmoi", "home-manager"] ]
display_time = true
assume_yes = true
nix_handler = "vanilla"
cleanup = true

